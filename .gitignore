# Teenage Engineering factory sample source — kept locally, not redistributed.
# The app only needs the processed copies in samples/.
PO ALL/
.DS_Store
node_modules/
*.log

# Raw art packs — only the extracted sprites in game/sprites/ are committed
Sprout Lands - Sprites - Basic pack/
summer_plains_v1.0_standard/
/tmp/
Farm RPG FREE 16x16 - Tiny Asset Pack/

# raw art pack - only the extracted sprites in game/sprites are committed
Mushroom/
